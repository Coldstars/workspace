# 后台表单与弹窗抽屉规范

## 表单
表单规格必须定义：
- 字段名称、类型、是否必填、默认值。
- 校验规则与错误文案。
- 保存、取消、重置、提交后的反馈。
- 是否影响列表、详情或统计数据。

## 弹窗
适合短确认、少量录入、危险操作二次确认。
必须包含：标题、说明、主按钮、次按钮、关闭方式、成功/失败反馈。

## 抽屉
适合详情、日志、复杂编辑、侧向流程。
必须包含：触发入口、展示字段、关闭策略、与当前页面的联动。

## 禁止
- 用弹窗承载过长表单。
- 危险操作没有确认。
- 表单校验只改变边框颜色，没有文字说明。
